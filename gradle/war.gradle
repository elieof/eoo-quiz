apply plugin: "war"

bootWar {
    mainClassName = "com.fahkap.eoo.quiz.EooquizApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
